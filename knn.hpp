/*
*	KNN classifier
*/

#include <vector>

template <typename T>
class KNN{

private:

	int euclidean_distance(const vector<T> &f1, \
		const vector<T> &f2, int size){

	}

public:
	void setup(int k){

	}

	void classify(const vector<T> &feature, const vector<vector<T> >train_data){
	}

};